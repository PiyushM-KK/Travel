# Secrets — never commit (real API keys live here)
server/.env

# OS / editor noise
.DS_Store
__pycache__/
*.pyc

# (from remote)
uploads/
.venv/
.image-slots.state.json
.thumbnail

# Local secrets — never commit
.env
.claude/
