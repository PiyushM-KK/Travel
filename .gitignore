# Secrets — never commit (real API keys live here)
server/.env

# OS / editor noise
.DS_Store
__pycache__/
*.pyc

# (from remote)
uploads/
.venv/
.image-slots.state.json
.thumbnail

# Local secrets — never commit
.env
.claude/

# social-automation (vendored firm platform)
node_modules/
social-automation/node_modules/
social-automation/.env

# Vercel project link (per-machine)
.vercel/
social-automation/.vercel/

# encrypted secrets — never commit (public repo)
.env.enc
social-automation/.env.enc
.vercel
.env*

# Private security audit reports — documents live vulns; NEVER commit to this PUBLIC repo
SECURITY-AUDIT-*.md

# Vendor rate sheets — confidential B2B net pricing; NEVER commit to this PUBLIC repo.
# The chatbot reads these locally/at upload; only Skyline SELL prices ever reach the site.
social-automation/assets/*Ready Reckoner*
social-automation/assets/*ratesheet*
social-automation/assets/*rate-sheet*
*.ratesheet.pdf
