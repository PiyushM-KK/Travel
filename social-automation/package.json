{
  "name": "skyline-social-automation",
  "version": "1.1.0",
  "private": true,
  "description": "Skyline Travel Planner — social content generation + publishing (Instagram + Facebook), grounded in Skyline's own packages and fact-checked. Engine + automation framework vendored from the firm's reusable platform; Skyline's own facts/voice/creds.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node tests/check_skyline_social.js && node tests/check_skyline_automation.js && node tests/check_airtable_imagesource.js && node tests/check_ab_selection.js && node tests/check_calendar_cards.js && node tests/check_stale_draft_reaper.js && node tests/check_claim_atomicity.js && node tests/check_package_posts.js && node tests/check_parse_decision.js && node tests/check_ops_status.js && node tests/check_generate_deadline.js && node tests/check_reseller.js && node tests/check_pkgcard_fix.js",
    "prep": "node automation/run.js prep",
    "publish-pass": "node automation/run.js publish",
    "verify-live": "node automation/verify-live.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.57.0",
    "@resvg/resvg-js": "^2.6.2",
    "jimp": "^1.6.1",
    "satori": "^0.29.0"
  },
  "optionalDependencies": {
    "@vercel/blob": "^2.6.1",
    "google-auth-library": "^11.0.0",
    "imapflow": "^1.0.0",
    "mailparser": "^3.6.0",
    "nodemailer": "^9.0.3"
  }
}
